# 🏋️ برنامج Sport Zone لإدارة النادي الرياضي

## 📖 دليل التثبيت والتشغيل الشامل

---

## ⚙️ المتطلبات الأساسية

قبل البدء، تأكد من تثبيت:

### 1. Node.js (الإصدار 18 أو أحدث)
**لتحميل Node.js:**
- اذهب إلى: https://nodejs.org
- حمّل النسخة LTS (الموصى بها)
- ثبّت البرنامج واتبع التعليمات

**للتحقق من التثبيت:**
```bash
node --version
# يجب أن يظهر: v18.x.x أو أحدث
```

---

## 🚀 خطوات التثبيت

### الخطوة 1: فك ضغط الملف

**على Windows:**
- انقر بزر الفأرة الأيمن على `sport-zone-gym.tar.gz`
- اختر "Extract All" أو استخدم برنامج WinRAR أو 7-Zip

**على Mac/Linux:**
```bash
tar -xzf sport-zone-gym.tar.gz
cd sport-zone-project
```

---

### الخطوة 2: فتح Terminal/CMD في مجلد المشروع

**على Windows:**
1. افتح المجلد `sport-zone-project`
2. اضغط `Shift + Right Click` في المجلد
3. اختر "Open PowerShell window here" أو "Open command window here"

**على Mac:**
1. افتح Terminal
2. اكتب: `cd ` (مع مسافة)
3. اسحب مجلد المشروع إلى Terminal
4. اضغط Enter

**على Linux:**
```bash
cd /path/to/sport-zone-project
```

---

### الخطوة 3: تثبيت المكتبات

في Terminal/CMD اكتب:

```bash
npm install
```

⏱️ **الوقت المتوقع:** 2-3 دقائق  
📊 **حجم التحميل:** حوالي 200-300 MB

**ماذا يحدث الآن؟**
- يتم تحميل جميع المكتبات اللازمة
- يتم تثبيت Next.js, Prisma, React وغيرها
- ستظهر لك رسائل في الشاشة (هذا طبيعي)

---

### الخطوة 4: إعداد قاعدة البيانات

```bash
npm run setup
```

**ماذا يحدث الآن؟**
✅ إنشاء قاعدة البيانات (SQLite)  
✅ إنشاء جميع الجداول  
✅ إنشاء مستخدم Admin  
✅ إنشاء الباقات الافتراضية  
✅ إنشاء الإعدادات الأولية  

**النتيجة المتوقعة:**
```
🌱 بدء إعداد قاعدة البيانات...
✅ تم إنشاء المستخدم: admin@sportzone.com
✅ تم إنشاء الباقات الافتراضية
✅ تم إنشاء الإعدادات الافتراضية

🎉 تم إعداد قاعدة البيانات بنجاح!

📧 البريد الإلكتروني: admin@sportzone.com
🔑 كلمة المرور: admin123
```

---

### الخطوة 5: تشغيل البرنامج

```bash
npm run dev
```

**النتيجة المتوقعة:**
```
✓ Ready in 2.5s
○ Local:    http://localhost:3000
```

🎉 **ممتاز! البرنامج يعمل الآن!**

---

## 🌐 فتح البرنامج

1. افتح المتصفح (Chrome, Firefox, Safari, Edge)
2. اذهب إلى: **http://localhost:3000**
3. أدخل بيانات الدخول:
   - **البريد:** admin@sportzone.com
   - **الباسورد:** admin123

---

## 📊 واجهة البرنامج

### الصفحة الرئيسية تحتوي على:

#### 1️⃣ الإجراءات السريعة
- 🔵 **تسجيل مشترك جديد** - لإضافة عضو جديد
- 🟢 **تسجيل حضور** - لتسجيل دخول المشتركين
- 🟣 **تسجيل دفعة** - لتسجيل المدفوعات
- 🟠 **التقارير** - لعرض التقارير والإحصائيات

#### 2️⃣ الإحصائيات
- **إجمالي المشتركين** - عدد جميع الأعضاء
- **حضور اليوم** - عدد من حضر اليوم
- **اشتراكات منتهية** - تحتاج تجديد
- **إيرادات الشهر** - المبلغ المحصل
- **مدفوعات معلقة** - تحتاج متابعة

#### 3️⃣ النشاط الأخير
- آخر المشتركين المسجلين
- آخر عمليات الحضور

---

## 🗂️ هيكل قاعدة البيانات

### الجداول الأساسية:

1. **User (المستخدمين)**
   - الموظفين والمدراء
   - نظام الصلاحيات

2. **Member (المشتركين)**
   - بيانات العضو الشخصية
   - رقم العضوية
   - الحالة (نشط/غير نشط)

3. **Package (الباقات)**
   - الباقة الشهرية (30 يوم - 200 ريال)
   - الباقة ربع السنوية (90 يوم - 500 ريال)
   - الباقة نصف السنوية (180 يوم - 900 ريال)
   - الباقة السنوية (365 يوم - 1600 ريال)

4. **Subscription (الاشتراكات)**
   - اشتراك العضو الحالي
   - تاريخ البدء والانتهاء
   - الحالة

5. **Attendance (الحضور)**
   - سجل الحضور اليومي
   - وقت الدخول والخروج

6. **Payment (المدفوعات)**
   - سجل جميع المدفوعات
   - رقم الإيصال
   - طريقة الدفع

---

## 🛠️ الأوامر المفيدة

```bash
# تشغيل البرنامج للتطوير
npm run dev

# بناء نسخة للإنتاج
npm run build

# تشغيل نسخة الإنتاج
npm start

# فتح Prisma Studio (إدارة قاعدة البيانات)
npm run db:studio

# تحديث قاعدة البيانات
npm run db:push

# إعادة ملء البيانات الأولية
npm run db:seed
```

---

## 🔐 الأمان

- ✅ كلمات المرور مشفرة (bcrypt)
- ✅ جلسات آمنة (NextAuth.js)
- ✅ قاعدة البيانات محلية (SQLite)
- ✅ لا يوجد اتصال بالإنترنت

**تغيير كلمة المرور:**
1. افتح: `prisma/seed.js`
2. غيّر: `admin123` إلى كلمة مرورك
3. احذف قاعدة البيانات: `rm prisma/dev.db`
4. أعد الإنشاء: `npm run setup`

---

## 📁 مكان حفظ البيانات

جميع البيانات محفوظة في:
```
prisma/dev.db
```

**⚠️ مهم جداً:**
- احفظ نسخة احتياطية من هذا الملف يومياً
- انسخه إلى مكان آمن
- يمكنك نقله إلى جهاز آخر مع المشروع

---

## 🔄 النسخ الاحتياطي

### طريقة سهلة:
```bash
# إنشاء نسخة احتياطية
cp prisma/dev.db prisma/backup-$(date +%Y%m%d).db

# استرجاع نسخة احتياطية
cp prisma/backup-20241109.db prisma/dev.db
```

---

## 🆘 حل المشاكل الشائعة

### 1. المشكلة: `npm: command not found`
**الحل:** Node.js غير مثبت
- حمّل وثبّت Node.js من https://nodejs.org

### 2. المشكلة: `Error: Cannot find module`
**الحل:**
```bash
rm -rf node_modules package-lock.json
npm install
```

### 3. المشكلة: البرنامج لا يفتح في المتصفح
**الحل:**
- تأكد أن البرنامج يعمل في Terminal
- جرب المنفذ: http://localhost:3000
- جرب 127.0.0.1:3000

### 4. المشكلة: خطأ في قاعدة البيانات
**الحل:**
```bash
rm prisma/dev.db
npm run setup
```

### 5. المشكلة: البرنامج بطيء
**الحل:**
- أغلق البرامج الثقيلة الأخرى
- استخدم متصفح Chrome أو Edge
- تأكد من مساحة القرص الصلب

---

## 📱 المراحل القادمة

### المرحلة 2 (قريباً):
- ✨ صفحة إضافة مشترك جديد (نموذج كامل)
- ✨ صفحة عرض جميع المشتركين
- ✨ البحث والتصفية

### المرحلة 3:
- ✨ نظام تسجيل الحضور (QR Code / رقم العضوية)
- ✨ تقرير الحضور اليومي

### المرحلة 4:
- ✨ إدارة الاشتراكات
- ✨ التجديد التلقائي
- ✨ إشعارات الانتهاء

### المرحلة 5:
- ✨ نظام المدفوعات
- ✨ طباعة الفواتير
- ✨ التقارير المالية

### المرحلة 6:
- ✨ التقارير المتقدمة
- ✨ الرسوم البيانية
- ✨ التصدير إلى Excel

### المرحلة 7:
- ✨ طباعة بطاقات العضوية
- ✨ نظام الإشعارات (واتساب/SMS)
- ✨ التكامل مع أجهزة البصمة

---

## 💻 المتطلبات التقنية

### الحد الأدنى:
- معالج: Intel Core i3 أو ما يعادله
- ذاكرة: 4 GB RAM
- مساحة: 500 MB فارغة
- نظام التشغيل: Windows 10, macOS 10.15, Linux

### الموصى به:
- معالج: Intel Core i5 أو أحدث
- ذاكرة: 8 GB RAM
- مساحة: 1 GB فارغة
- SSD لأداء أفضل

---

## 📞 الدعم

هل واجهت مشكلة؟ 
- راجع قسم "حل المشاكل الشائعة" أعلاه
- تأكد من اتباع جميع الخطوات بالترتيب
- اقرأ الرسائل التي تظهر في Terminal

---

## 🎉 تهانينا!

أنت الآن جاهز لاستخدام برنامج Sport Zone لإدارة نادي الرياضي! 

**التالي:**
- جرّب إضافة مشترك جديد
- سجّل حضور
- استكشف الإحصائيات

**استمتع باستخدام البرنامج! 🚀**

---

📌 **ملاحظة مهمة:**
- البرنامج يعمل offline بالكامل
- لا يحتاج اتصال بالإنترنت
- جميع البيانات محفوظة محلياً على جهازك
- آمن وسريع

---

© 2024 Sport Zone Management System
