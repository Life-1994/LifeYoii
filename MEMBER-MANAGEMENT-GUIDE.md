# نظام إدارة الأعضاء والاشتراكات المتقدم
## Advanced Member & Subscription Management System

## 📋 نظرة عامة

تم تطوير نظام شامل ومتقدم لإدارة الأعضاء والاشتراكات في النادي الرياضي مع جميع الميزات المطلوبة.

## ✨ الميزات المطبقة

### 1. قائمة الأعضاء (Members List)
✅ جدول شامل مع جميع بيانات الأعضاء
✅ بحث متقدم: بالاسم، البريد، الهاتف، رقم العضوية
✅ تصفية متقدمة: حسب الحالة، الجنس، حالة الاشتراك
✅ اختيار متعدد (Checkboxes) للعمليات الجماعية
✅ Pagination: 10 عناصر لكل صفحة
✅ عرض عدد الحضور والمدفوعات لكل عضو

**الملف**: `src/app/members/page.tsx`

### 2. ملف العضو الشامل (Member Profile)
✅ معلومات عامة: الصورة، الاسم، البريد، الهاتف، العنوان
✅ معلومات العضوية: رقم العضوية، تاريخ الانضمام، QR Code
✅ الاشتراك الحالي: النوع، التواريخ، الأيام المتبقية
✅ Tabs للتنقل بين:
  - نظرة عامة (Overview)
  - سجل الاشتراكات
  - سجل الحضور (آخر 30 يوم)
  - سجل المدفوعات
✅ إحصائيات سريعة

**الملف**: `src/app/members/[id]/page.tsx`

### 3. QR Code System
✅ QR Code فريد لكل عضو
✅ عرض وتحميل QR Code
✅ مسح QR Code للوصول السريع لملف العضو
✅ إدخال يدوي لرمز QR

**المكونات**:
- `src/components/members/QRCodeDisplay.tsx`
- `src/components/members/QRScanner.tsx`

### 4. تصدير البيانات
✅ تصدير بصيغة Excel (.xlsx)
✅ تصدير بصيغة CSV (.csv)
✅ تصدير بصيغة JSON (.json)
✅ تطبيق الفلاتر قبل التصدير

**المكون**: `src/components/members/ExportDialog.tsx`
**API**: `src/app/api/members/export/route.ts`

### 5. العمليات الجماعية
✅ تغيير حالة متعددة
✅ حذف متعدد (Soft Delete)
✅ إرسال إشعار جماعي
✅ تجديد اشتراكات جماعي
✅ قائمة عائمة تظهر عند اختيار أعضاء

**المكون**: `src/components/members/BulkActionsMenu.tsx`
**API**: `src/app/api/members/bulk/route.ts`

### 6. إدارة الاشتراكات
✅ قائمة جميع الاشتراكات
✅ تصفية حسب الحالة (نشط، منتهي، ملغي، مجمد)
✅ صفحة خاصة للاشتراكات المنتهية (خلال 7 أيام)
✅ عرض الأيام المتبقية
✅ تنبيهات للاشتراكات المنتهية

**الصفحات**:
- `src/app/subscriptions/page.tsx`
- `src/app/subscriptions/expiring/page.tsx`

**المكون**: `src/components/subscriptions/SubscriptionCard.tsx`

## 🗄️ قاعدة البيانات

### التحديثات على Prisma Schema

#### جدول الأعضاء (Member)
```prisma
- healthConditions: String?  // أمراض مزمنة، حساسية
- qrCode: String? @unique    // QR Code فريد
- deletedAt: DateTime?       // Soft delete
- measurements: BodyMeasurement[]
```

#### جدول الاشتراكات (Subscription)
```prisma
- discount: Float @default(0)
- paymentStatus: String @default("paid")
- paymentMethod: String?
- freezeReason: String?
- freezeStartDate: DateTime?
- freezeEndDate: DateTime?
- cancelReason: String?
```

#### جداول جديدة

**BodyMeasurement**: القياسات الجسمية
```prisma
model BodyMeasurement {
  id          String    @id @default(cuid())
  memberId    String
  weight      Float?
  height      Float?
  bmi         Float?
  bodyFat     Float?
  muscle      Float?
  notes       String?
  member      Member    @relation(...)
  createdAt   DateTime  @default(now())
}
```

**Notification**: الإشعارات
```prisma
model Notification {
  id          String    @id @default(cuid())
  type        String
  title       String
  message     String
  recipientId String?
  isRead      Boolean   @default(false)
  createdAt   DateTime  @default(now())
}
```

## 🔌 API Routes

### Members APIs

#### الأساسية
- `GET /api/members` - قائمة الأعضاء مع البحث والتصفية
- `POST /api/members` - إضافة عضو جديد
- `GET /api/members/[id]` - تفاصيل عضو
- `PATCH /api/members/[id]` - تعديل عضو
- `DELETE /api/members/[id]` - حذف عضو

#### المتقدمة
- `GET /api/members/[id]/subscriptions` - اشتراكات العضو
- `GET /api/members/[id]/attendance` - سجل حضور العضو
- `GET /api/members/[id]/payments` - سجل مدفوعات العضو
- `GET /api/members/[id]/qrcode` - توليد QR Code
- `POST /api/members/[id]/qrcode` - مسح QR Code
- `GET /api/members/export` - تصدير البيانات
- `POST /api/members/bulk` - عمليات جماعية

### Subscriptions APIs

- `GET /api/subscriptions` - قائمة الاشتراكات
- `POST /api/subscriptions` - إضافة اشتراك
- `GET /api/subscriptions/[id]` - تفاصيل اشتراك
- `PATCH /api/subscriptions/[id]` - تعديل اشتراك
- `DELETE /api/subscriptions/[id]` - حذف اشتراك
- `POST /api/subscriptions/renew` - تجديد اشتراك
- `POST /api/subscriptions/freeze` - تجميد اشتراك
- `PATCH /api/subscriptions/freeze` - إلغاء التجميد

## 📦 المكتبات المستخدمة

```json
{
  "qrcode": "^1.5.3",           // توليد QR Codes
  "html5-qrcode": "^2.3.8",     // مسح QR Codes
  "@tanstack/react-table": "^8.11.0",  // جداول متقدمة
  "xlsx": "^0.18.5",            // تصدير Excel
  "jspdf": "^2.5.1",            // تصدير PDF
  "date-fns": "^3.0.0"          // معالجة التواريخ
}
```

## 🎨 UI/UX Features

✅ Responsive Design - يعمل على جميع الأحجام
✅ Dark Mode Ready
✅ Loading Skeletons
✅ Empty States
✅ Toast Notifications
✅ Confirmation Dialogs
✅ Modal Dialogs
✅ Status Badges ملونة
✅ Avatar Fallbacks
✅ Print-Friendly

## 🚀 الاستخدام

### تشغيل المشروع

```bash
# تثبيت المكتبات
npm install

# إعداد قاعدة البيانات
npm run setup

# تشغيل البيئة التطويرية
npm run dev

# بناء للإنتاج
npm run build
```

### الوصول للصفحات

- **قائمة الأعضاء**: `/members`
- **إضافة عضو**: `/members/new`
- **ملف عضو**: `/members/[id]`
- **قائمة الاشتراكات**: `/subscriptions`
- **اشتراكات منتهية**: `/subscriptions/expiring`

## 📊 أمثلة الاستخدام

### 1. البحث عن عضو
1. اذهب إلى `/members`
2. استخدم شريط البحث للبحث بالاسم أو رقم الهاتف
3. أو استخدم الفلاتر المتقدمة

### 2. تصدير البيانات
1. في صفحة الأعضاء، اضغط على زر "تصدير"
2. اختر الصيغة (Excel, CSV, JSON)
3. سيتم تحميل الملف تلقائياً

### 3. مسح QR Code
1. اضغط على أيقونة QR في الهيدر
2. أدخل رمز QR يدوياً
3. سيتم فتح ملف العضو مباشرة

### 4. العمليات الجماعية
1. حدد الأعضاء باستخدام Checkboxes
2. ستظهر قائمة عائمة في الأسفل
3. اختر العملية المطلوبة (تفعيل، إيقاف، حذف، إرسال إشعار)

### 5. عرض الاشتراكات المنتهية
1. اذهب إلى `/subscriptions/expiring`
2. ستجد جميع الاشتراكات التي تنتهي خلال 7 أيام
3. يمكنك التجديد مباشرة

## 🔐 الأمان

- ✅ Soft Delete للأعضاء (يمكن الاستعادة)
- ✅ Confirmation Dialogs للعمليات الحساسة
- ✅ Validation على جميع النماذج
- ✅ QR Codes فريدة لكل عضو
- ✅ حماية API Routes

## 📈 الأداء

- ✅ Pagination للقوائم الطويلة
- ✅ Lazy Loading للصور
- ✅ Optimized Queries
- ✅ Client-side Caching
- ✅ يدعم 1000+ عضو بسلاسة

## 🐛 المشاكل المعروفة وحلولها

### مشكلة: البناء يفشل
**الحل**: تأكد من تشغيل `npx prisma generate` قبل البناء

### مشكلة: QR Code لا يعمل
**الحل**: تحقق من وجود المكتبة `qrcode` في dependencies

## 🔄 التحديثات المستقبلية

- [ ] نظام الحجوزات (Bookings)
- [ ] تقارير متقدمة
- [ ] إرسال SMS/Email للإشعارات
- [ ] تطبيق موبايل للأعضاء
- [ ] نظام نقاط الولاء
- [ ] تكامل مع بوابات الدفع

## 📞 الدعم

للأسئلة والمساعدة، يرجى فتح Issue في GitHub.

## 📝 الترخيص

هذا المشروع مرخص تحت MIT License.

---

**تم التطوير بواسطة**: GitHub Copilot
**التاريخ**: نوفمبر 2024
**الإصدار**: 2.0.0
