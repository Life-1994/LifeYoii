# دليل الإعداد السريع / Quick Setup Guide
## نظام الدفع والفواتير المتقدم

## 🚀 البدء السريع / Quick Start

### 1. تثبيت المتطلبات / Install Dependencies

```bash
cd /path/to/LifeYoii
npm install
```

### 2. إعداد قاعدة البيانات / Setup Database

```bash
# Push schema to database
npm run db:push

# (Optional) Seed initial data
npm run db:seed
```

### 3. تكوين متغيرات البيئة / Configure Environment Variables

ملف `.env` موجود بالفعل مع الإعدادات الأساسية. للتكامل مع Stripe (اختياري):

```env
# Stripe Test Keys (Optional - for payment gateway testing)
STRIPE_SECRET_KEY="sk_test_your_key_here"
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_your_key_here"
STRIPE_WEBHOOK_SECRET="whsec_your_webhook_secret"
```

💡 **ملاحظة**: يمكنك استخدام النظام بدون Stripe - الدفع النقدي والبطاقات يعمل مباشرة!

### 4. تشغيل التطبيق / Run Application

```bash
# Development mode
npm run dev

# Production build
npm run build
npm start
```

افتح المتصفح: http://localhost:3000

## 📍 الصفحات المتاحة / Available Pages

### الصفحات الرئيسية / Main Pages
- **الرئيسية**: http://localhost:3000
- **الأعضاء**: http://localhost:3000/members
- **المدفوعات**: http://localhost:3000/payments
- **الفواتير**: http://localhost:3000/invoices
- **الكوبونات**: http://localhost:3000/coupons
- **التقارير المالية**: http://localhost:3000/financial-reports

## 💳 اختبار المميزات / Testing Features

### اختبار الدفع النقدي / Testing Cash Payment

1. انتقل إلى صفحة الأعضاء
2. اختر عضو أو أنشئ عضو جديد
3. في صفحة تفاصيل العضو، قم بإنشاء اشتراك
4. سيتم تسجيل الدفعة تلقائياً
5. تحقق من الدفعة في صفحة المدفوعات

### اختبار الفواتير / Testing Invoices

#### إنشاء فاتورة يدوياً / Create Invoice Manually

```bash
# استخدم API مباشرة
curl -X POST http://localhost:3000/api/invoices \
  -H "Content-Type: application/json" \
  -d '{
    "memberId": "member_id_here",
    "items": [
      {
        "description": "Monthly Membership",
        "quantity": 1,
        "unitPrice": 500
      }
    ],
    "discount": 0,
    "tax": 0,
    "dueDate": "2025-12-31"
  }'
```

أو استخدم صفحة الفواتير لعرض الفواتير الموجودة.

### اختبار الكوبونات / Testing Coupons

1. انتقل إلى http://localhost:3000/coupons
2. اضغط "إضافة كوبون جديد"
3. املأ النموذج:
   - الكود: `WELCOME50`
   - النوع: نسبة مئوية
   - القيمة: 50
   - الصلاحية: اختر التواريخ
4. احفظ الكوبون
5. اختبر التحقق من صحة الكوبون:

```bash
curl -X POST http://localhost:3000/api/coupons/WELCOME50/validate \
  -H "Content-Type: application/json" \
  -d '{"amount": 1000}'
```

### اختبار PDF الفاتورة / Testing Invoice PDF

1. انتقل إلى صفحة الفواتير
2. اختر فاتورة واضغط "عرض"
3. اضغط "تحميل PDF"
4. سيتم تنزيل ملف PDF ثنائي اللغة (عربي/إنجليزي)

### عرض التقارير المالية / View Financial Reports

1. انتقل إلى http://localhost:3000/financial-reports
2. اختر الفترة الزمنية (يومي/أسبوعي/شهري)
3. شاهد:
   - إجمالي الإيرادات
   - المدفوعات المعلقة
   - المبالغ المستردة
   - عدد المدفوعات المكتملة

## 🔧 استكشاف الأخطاء / Troubleshooting

### المشكلة: لا توجد بيانات في التقارير
**الحل**: تأكد من وجود مدفوعات مكتملة في قاعدة البيانات

```bash
# افحص قاعدة البيانات
npm run db:studio
```

### المشكلة: خطأ في البناء (Build Error)
**الحل**: تأكد من تثبيت جميع الحزم

```bash
npm install
npm run build
```

### المشكلة: خطأ في قاعدة البيانات
**الحل**: أعد إنشاء قاعدة البيانات

```bash
rm prisma/dev.db
npm run db:push
```

## 📊 بيانات تجريبية / Sample Data

لإنشاء بيانات تجريبية للاختبار، يمكنك:

### إنشاء دفعة تجريبية / Create Sample Payment

```bash
curl -X POST http://localhost:3000/api/payments \
  -H "Content-Type: application/json" \
  -d '{
    "memberId": "your_member_id",
    "amount": 500,
    "currency": "SAR",
    "method": "CASH",
    "description": "Monthly subscription payment"
  }'
```

### إنشاء كوبون تجريبي / Create Sample Coupon

```bash
curl -X POST http://localhost:3000/api/coupons \
  -H "Content-Type: application/json" \
  -d '{
    "code": "DISCOUNT20",
    "type": "PERCENTAGE",
    "value": 20,
    "minAmount": 100,
    "validFrom": "2025-01-01T00:00:00Z",
    "validUntil": "2025-12-31T23:59:59Z"
  }'
```

## 🔐 Stripe Integration (اختياري / Optional)

### للاختبار مع Stripe / To Test with Stripe

1. سجل حساب Stripe: https://stripe.com
2. احصل على مفاتيح الاختبار من Dashboard
3. أضف المفاتيح إلى `.env`
4. استخدم بطاقات الاختبار:
   - نجاح: `4242 4242 4242 4242`
   - فشل: `4000 0000 0000 0002`
   - تاريخ انتهاء: أي تاريخ مستقبلي
   - CVC: أي 3 أرقام

## 📱 الوصول عبر الشبكة / Network Access

لاختبار من أجهزة أخرى على نفس الشبكة:

```bash
# Find your IP
ipconfig  # Windows
ifconfig  # Mac/Linux

# Access from other devices
http://YOUR_IP:3000
```

## 🎯 نصائح الاختبار / Testing Tips

1. **ابدأ بالدفع النقدي**: أسهل طريقة للاختبار
2. **أنشئ فواتير أولاً**: قبل اختبار الدفعات المرتبطة
3. **استخدم الكوبونات**: اختبر مع مبالغ مختلفة
4. **راجع التقارير**: بعد كل عملية للتأكد من التسجيل
5. **جرب PDF**: تأكد من عمل التنزيل بشكل صحيح

## 📚 موارد إضافية / Additional Resources

- **الدليل الكامل**: راجع `PAYMENT-SYSTEM-README.md`
- **ملخص التنفيذ**: راجع `IMPLEMENTATION-SUMMARY.md`
- **Prisma Studio**: استخدم `npm run db:studio` لعرض البيانات
- **Next.js Docs**: https://nextjs.org/docs
- **Stripe Docs**: https://stripe.com/docs

## 🆘 الدعم / Support

إذا واجهت أي مشاكل:
1. راجع قسم استكشاف الأخطاء أعلاه
2. افحص console logs في المتصفح
3. افحص terminal logs من الخادم
4. راجع ملفات التوثيق
5. أنشئ issue في المستودع

---

## ✅ قائمة التحقق / Checklist

قبل البدء في الاختبار، تأكد من:

- [ ] تم تثبيت Node.js (v16+)
- [ ] تم تشغيل `npm install`
- [ ] تم تشغيل `npm run db:push`
- [ ] ملف `.env` موجود
- [ ] التطبيق يعمل على port 3000
- [ ] يمكن الوصول إلى الصفحة الرئيسية
- [ ] توجد بيانات أعضاء للاختبار

**جاهز للاختبار! 🚀**
