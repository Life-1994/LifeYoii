# 🚀 دليل البدء السريع - نظام إدارة الأعضاء

## التثبيت والتشغيل ⚡

```bash
# 1. تثبيت المكتبات
npm install

# 2. إعداد قاعدة البيانات
npm run setup

# 3. تشغيل التطوير
npm run dev

# 4. فتح المتصفح
# http://localhost:3000
```

## بيانات الدخول الافتراضية 🔐

- **البريد**: `admin@sportzone.com`
- **كلمة المرور**: `admin123`

## الصفحات الرئيسية 📱

| الصفحة | المسار | الوصف |
|--------|-------|-------|
| الرئيسية | `/` | لوحة التحكم |
| قائمة الأعضاء | `/members` | جميع الأعضاء |
| إضافة عضو | `/members/new` | تسجيل عضو جديد |
| ملف العضو | `/members/[id]` | تفاصيل عضو |
| الاشتراكات | `/subscriptions` | جميع الاشتراكات |
| اشتراكات منتهية | `/subscriptions/expiring` | تنتهي خلال 7 أيام |

## الميزات الرئيسية ⭐

### 1. إدارة الأعضاء
- ✅ قائمة شاملة مع بحث وفلترة
- ✅ إضافة/تعديل/حذف
- ✅ ملف عضو شامل
- ✅ QR Code لكل عضو

### 2. QR Code System
- في صفحة الأعضاء: اضغط على أيقونة QR في الهيدر
- في ملف العضو: QR Code في الجانب الأيسر
- يمكن تحميل QR Code كصورة

### 3. تصدير البيانات
- في صفحة الأعضاء: زر "تصدير"
- اختر الصيغة: Excel, CSV, JSON
- التنزيل تلقائي

### 4. العمليات الجماعية
- اختر عدة أعضاء بالـ checkboxes
- ستظهر قائمة في الأسفل
- الخيارات: تفعيل، إيقاف، حذف، إرسال إشعار

### 5. إدارة الاشتراكات
- عرض جميع الاشتراكات
- فلترة حسب الحالة
- صفحة خاصة للمنتهية قريباً

## API Endpoints 🔌

### Members
```
GET    /api/members              قائمة الأعضاء
POST   /api/members              إضافة عضو
GET    /api/members/[id]         تفاصيل عضو
PATCH  /api/members/[id]         تعديل عضو
DELETE /api/members/[id]         حذف عضو
GET    /api/members/export       تصدير البيانات
POST   /api/members/bulk         عمليات جماعية
```

### QR Code
```
GET    /api/members/[id]/qrcode  توليد QR
POST   /api/members/[id]/qrcode  مسح QR
```

### Subscriptions
```
GET    /api/subscriptions        قائمة الاشتراكات
POST   /api/subscriptions        إضافة اشتراك
GET    /api/subscriptions/[id]   تفاصيل اشتراك
POST   /api/subscriptions/renew  تجديد اشتراك
POST   /api/subscriptions/freeze تجميد اشتراك
```

## المكونات المتاحة 🧩

```tsx
// فلاتر البحث
import MemberFilters from '@/components/members/MemberFilters'

// QR Code
import QRCodeDisplay from '@/components/members/QRCodeDisplay'
import QRScanner from '@/components/members/QRScanner'

// تصدير
import ExportDialog from '@/components/members/ExportDialog'

// عمليات جماعية
import BulkActionsMenu from '@/components/members/BulkActionsMenu'

// بطاقة اشتراك
import SubscriptionCard from '@/components/subscriptions/SubscriptionCard'
```

## أمثلة الاستخدام 💡

### إضافة عضو جديد
1. اذهب إلى `/members`
2. اضغط "مشترك جديد"
3. املأ النموذج
4. اختر الباقة (اختياري)
5. احفظ

### البحث عن عضو
1. في صفحة الأعضاء
2. استخدم شريط البحث
3. أو اضغط "إظهار الفلاتر المتقدمة"
4. اختر الفلاتر المطلوبة

### مسح QR Code
1. اضغط أيقونة QR في الهيدر
2. أدخل الرمز: `MEMBER:M1001:xxxxx`
3. أو استخدم كاميرا الجوال
4. سيفتح ملف العضو

### تصدير البيانات
1. في صفحة الأعضاء
2. اختر الفلاتر إن أردت
3. اضغط "تصدير"
4. اختر الصيغة
5. سيتم التحميل تلقائياً

### عمليات جماعية
1. حدد عدة أعضاء
2. ستظهر قائمة في الأسفل
3. اختر العملية
4. أكد العملية

## البنية الأساسية 📁

```
src/
├── app/
│   ├── api/
│   │   ├── members/          # API الأعضاء
│   │   └── subscriptions/    # API الاشتراكات
│   ├── members/              # صفحات الأعضاء
│   └── subscriptions/        # صفحات الاشتراكات
├── components/
│   ├── members/              # مكونات الأعضاء
│   └── subscriptions/        # مكونات الاشتراكات
└── lib/
    ├── prisma.ts             # Prisma client
    └── auth.ts               # المصادقة
```

## قاعدة البيانات 🗄️

### الجداول الرئيسية
- **Member** - الأعضاء
- **Subscription** - الاشتراكات
- **Package** - الباقات
- **Attendance** - الحضور
- **Payment** - المدفوعات
- **BodyMeasurement** - القياسات الجسمية
- **Notification** - الإشعارات

### تحديث Schema
```bash
npx prisma generate
npx prisma db push
```

## المشاكل الشائعة 🔧

### المشكلة: Build يفشل
```bash
# الحل
npx prisma generate
npm run build
```

### المشكلة: QR لا يعمل
```bash
# تأكد من تثبيت المكتبة
npm install qrcode
```

### المشكلة: التصدير لا يعمل
```bash
# تأكد من تثبيت xlsx
npm install xlsx
```

## النسخ الاحتياطي 💾

```bash
# نسخ احتياطي لقاعدة البيانات
cp prisma/dev.db prisma/dev.db.backup

# استعادة
cp prisma/dev.db.backup prisma/dev.db
```

## الأوامر المفيدة 🛠️

```bash
# تشغيل التطوير
npm run dev

# بناء الإنتاج
npm run build

# تشغيل الإنتاج
npm run start

# إعادة تهيئة قاعدة البيانات
npm run setup

# فتح Prisma Studio
npm run db:studio
```

## الدعم 📞

- **التوثيق الكامل**: `MEMBER-MANAGEMENT-GUIDE.md`
- **تقرير التنفيذ**: `IMPLEMENTATION-SUMMARY.md`
- **Issues**: GitHub Issues

## الترخيص 📄

MIT License

---

**النسخة**: 2.0.0
**آخر تحديث**: نوفمبر 2024
**الحالة**: ✅ جاهز للاستخدام
