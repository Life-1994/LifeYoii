# 🚀 دليل البدء السريع - Sport Zone

## خطوات التشغيل (5 دقائق فقط!)

### 1️⃣ فك الضغط
```bash
tar -xzf sport-zone-gym.tar.gz
cd sport-zone-project
```

### 2️⃣ تثبيت المكتبات
```bash
npm install
```
**ملاحظة:** هذه الخطوة قد تستغرق 2-3 دقائق في أول مرة

### 3️⃣ إعداد قاعدة البيانات
```bash
npm run setup
```
✅ سيتم إنشاء قاعدة البيانات والمستخدم الأول تلقائياً

### 4️⃣ تشغيل البرنامج
```bash
npm run dev
```

### 5️⃣ افتح المتصفح
```
http://localhost:3000
```

## 🔐 بيانات الدخول
```
البريد: admin@sportzone.com
الباسورد: admin123
```

---

## 📦 محتويات المشروع

```
sport-zone-project/
├── 📁 prisma/              # قاعدة البيانات
│   ├── schema.prisma       # تصميم الجداول
│   └── seed.js            # البيانات الأولية
│
├── 📁 src/
│   ├── 📁 app/            # صفحات البرنامج
│   │   ├── layout.tsx     # التصميم العام
│   │   ├── page.tsx       # الصفحة الرئيسية
│   │   └── globals.css    # الأنماط
│   │
│   └── 📁 lib/            # المكتبات
│       ├── prisma.ts      # اتصال قاعدة البيانات
│       └── auth.ts        # نظام المصادقة
│
├── 📁 public/             # الملفات العامة
│   └── logo.png          # شعار Sport Zone
│
├── 📄 .env                # إعدادات البرنامج
├── 📄 package.json        # المكتبات المطلوبة
└── 📄 README.md           # الدليل الكامل
```

---

## 🎯 المميزات الحالية

✅ لوحة تحكم احترافية  
✅ إحصائيات فورية  
✅ شعار Sport Zone  
✅ دعم كامل للعربية (RTL)  
✅ يعمل offline بدون إنترنت  
✅ قاعدة بيانات SQLite سريعة  

---

## 📱 المراحل التالية

سنضيف في المراحل القادمة:
1. ✨ صفحة تسجيل المشتركين
2. ✨ إدارة المشتركين (عرض/تعديل/حذف)
3. ✨ نظام تسجيل الحضور
4. ✨ إدارة الاشتراكات والتجديد
5. ✨ نظام المدفوعات والفواتير
6. ✨ التقارير والإحصائيات المتقدمة
7. ✨ نظام الإشعارات التلقائية
8. ✨ طباعة بطاقات العضوية
9. ✨ نسخ احتياطي تلقائي

---

## 🆘 مشاكل شائعة وحلولها

### المشكلة: خطأ في npm install
```bash
# الحل: احذف وأعد التثبيت
rm -rf node_modules package-lock.json
npm install
```

### المشكلة: البرنامج لا يعمل
```bash
# تأكد من تثبيت Node.js أولاً
node --version  # يجب أن يكون 18 أو أحدث

# ثم أعد تشغيل البرنامج
npm run dev
```

### المشكلة: خطأ في قاعدة البيانات
```bash
# احذف القاعدة وأعد الإنشاء
rm prisma/dev.db
npm run setup
```

---

## 📞 جاهز للبدء؟

1. ✅ فك الضغط
2. ✅ npm install
3. ✅ npm run setup
4. ✅ npm run dev
5. ✅ افتح http://localhost:3000

**🎉 مبروك! برنامجك جاهز للاستخدام**

---

💡 **نصيحة:** احفظ نسخة احتياطية من ملف `prisma/dev.db` بشكل دوري
