# 🏋️ Sport Zone - دليل التشغيل

## 📋 المتطلبات
- Node.js (الإصدار 18 أو أحدث)
- npm أو yarn

## 🚀 خطوات التشغيل

### 1️⃣ إنشاء المشروع

```bash
# إنشاء مجلد المشروع
mkdir sport-zone-gym
cd sport-zone-gym

# تهيئة المشروع
npm init -y
```

### 2️⃣ نسخ الملفات

انسخ جميع الملفات التي أرسلتها لك في الأماكن الصحيحة:

```
sport-zone-gym/
├── prisma/
│   ├── schema.prisma          (من ملف prisma-schema.prisma)
│   └── seed.js                (من ملف prisma-seed.js)
├── src/
│   ├── app/
│   │   ├── layout.tsx         (من ملف app-layout.tsx)
│   │   ├── page.tsx           (من ملف app-page.tsx)
│   │   └── globals.css        (من ملف app-globals.css)
│   └── lib/
│       ├── prisma.ts          (من ملف lib-prisma.ts)
│       └── auth.ts            (من ملف lib-auth.ts)
├── .env                       (من ملف .env)
├── package.json               (من ملف package.json)
└── public/
    └── logo.png              (ضع شعار Sport Zone هنا)
```

### 3️⃣ تثبيت المكتبات

```bash
npm install
```

### 4️⃣ إعداد قاعدة البيانات

```bash
# إنشاء قاعدة البيانات
npm run setup
```

هذا الأمر سيقوم بـ:
- ✅ إنشاء ملف قاعدة البيانات SQLite
- ✅ إنشاء الجداول
- ✅ إنشاء مستخدم admin
- ✅ إنشاء الباقات الافتراضية

### 5️⃣ تشغيل البرنامج

```bash
npm run dev
```

البرنامج سيعمل على: **http://localhost:3000**

## 🔐 بيانات الدخول الافتراضية

```
البريد الإلكتروني: admin@sportzone.com
كلمة المرور: admin123
```

## 📊 الأوامر المفيدة

```bash
# تشغيل البرنامج
npm run dev

# فتح Prisma Studio (لإدارة قاعدة البيانات)
npm run db:studio

# تحديث قاعدة البيانات
npm run db:push

# إعادة تعبئة البيانات الأولية
npm run db:seed
```

## 🎨 المميزات المتوفرة

### ✅ الإحصائيات الرئيسية
- إجمالي المشتركين
- حضور اليوم
- الاشتراكات المنتهية
- الإيرادات الشهرية

### ✅ الإجراءات السريعة
- تسجيل مشترك جديد
- تسجيل حضور
- تسجيل دفعة
- عرض التقارير

## 📱 المراحل التالية

بعد تشغيل البرنامج بنجاح، سنضيف:

1. **صفحة تسجيل المشتركين** - نموذج كامل لإضافة مشتركين
2. **صفحة إدارة المشتركين** - عرض وتعديل وحذف
3. **صفحة تسجيل الحضور** - نظام حضور سريع
4. **صفحة الاشتراكات** - إدارة وتجديد الاشتراكات
5. **صفحة المدفوعات** - تسجيل وطباعة الفواتير
6. **صفحة التقارير** - تقارير شاملة ورسوم بيانية
7. **نظام الإشعارات** - تنبيهات تلقائية للاشتراكات

## 🔧 ملاحظات مهمة

- ✅ البرنامج يعمل **offline** بالكامل (لا يحتاج إنترنت)
- ✅ قاعدة البيانات محفوظة في: `prisma/dev.db`
- ✅ يدعم اللغة العربية بالكامل (RTL)
- ✅ واجهة سريعة وسهلة الاستخدام
- ✅ متوافق مع شعار Sport Zone

## 🆘 حل المشاكل

### إذا واجهت مشكلة في التثبيت:
```bash
# احذف المجلدات وأعد التثبيت
rm -rf node_modules package-lock.json
npm install
```

### إذا واجهت مشكلة في قاعدة البيانات:
```bash
# احذف قاعدة البيانات وأعد الإنشاء
rm -rf prisma/dev.db
npm run setup
```

## 📞 الدعم

إذا واجهت أي مشكلة، أخبرني وسأساعدك فوراً! 🚀
